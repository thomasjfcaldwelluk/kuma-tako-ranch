export type ValuePoint = {
  icon: string;
  title: string;
  description: string;
};

export type About = {
  title: string;
  subtitle: string;
  description: string;
  images: string[];
  values: ValuePoint[];
};